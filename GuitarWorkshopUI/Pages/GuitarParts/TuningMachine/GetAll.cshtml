@page
@model GuitarWorkshopUI.Pages.GuitarParts.TuningMachine.GetAllModel
@{
    ViewData["Title"] = "Tuning Machines";
}

<h1>Tuning Machines</h1>

<p>
    <a asp-page="Create">Create New</a>  <!-- Optional: Link to a Create page -->
</p>

<table class="table">
    <tbody>
        @foreach (var item in Model.TuningMachines)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.MachineId)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.MachineName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Price)
                </td>
                <td>
                    <div style="display:inline-flex">
                        <a asp-page="./Edit" asp-route-id="@item.MachineId" class="btn btn-sm">Edit</a>
                        <form method="post" asp-page-handler="Delete" asp-route-id="@item.MachineId">
                            <button type="submit" class="btn btn-danger btn-sm">Delete</button>
                        </form>
                    </div>
                </td>
            </tr>
        }
    </tbody>
</table>
